# Stop Lombok from looking for further config files in parent directories.
config.stopBubbling = true

# Mark generated members with @lombok.Generated so JaCoCo and static analysis
# tools exclude them from coverage and lint.
lombok.addLombokGeneratedAnnotation = true
